const Employee = require('../lib/employee');
const newEmployee = new Employee('Damien','666','theOmen@fakeemail.com')

test 
('tests whether or not we can create a new employee NAME properly with Employee constructor', () => {
    expect().toBe();
    expect().toBe();
    expect().toBe();
});
